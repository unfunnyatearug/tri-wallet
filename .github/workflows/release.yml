name: release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Test
        run: cargo test --release

      - name: Build
        run: cargo build --release --features gui

      - name: Install NSIS
        run: choco install nsis -y --no-progress

      - name: Build installer
        run: |
          New-Item -ItemType Directory -Force dist
          & "C:\Program Files (x86)\NSIS\makensis.exe" installer\tri.nsi

      - name: Checksums
        run: |
          Copy-Item target\release\tri.exe dist\
          Copy-Item target\release\tri-gui.exe dist\
          Get-FileHash dist\*.exe -Algorithm SHA256 |
            ForEach-Object { "{0}  {1}" -f $_.Hash.ToLower(), (Split-Path $_.Path -Leaf) } |
            Set-Content dist\SHA256SUMS.txt -Encoding ascii
          Get-Content dist\SHA256SUMS.txt

      # Idempotent, so a tag can be rebuilt without the step failing on an
      # existing release.
      - name: Publish release
        run: |
          if (-not (gh release view "$env:GITHUB_REF_NAME" 2>$null)) {
            gh release create "$env:GITHUB_REF_NAME" --notes-file .github\RELEASE_NOTES.md
          }
          gh release upload "$env:GITHUB_REF_NAME" dist\tri-setup.exe dist\tri.exe dist\tri-gui.exe dist\SHA256SUMS.txt --clobber
        env:
          GH_TOKEN: ${{ github.token }}
